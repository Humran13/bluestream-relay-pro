"""BlueStream Relay Pro - local Flask console package (GUI-1A.2).

Read-only authenticated console foundation. Deployment integration (nginx,
sudoers, systemd, /etc/bluestream) is deliberately deferred to later phases.
"""

__version__ = "0.1.0"
