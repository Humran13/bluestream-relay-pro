    # BlueStream bundled web player (hls.js).
    location ^~ /player/ {
        alias __WEB_ROOT__/;
        index index.html;
        add_header Access-Control-Allow-Origin "*" always;
        add_header X-Content-Type-Options nosniff always;
        try_files $uri $uri/ =404;
    }
